---
import Layout from "../layouts/Layout.astro";
import Dashboard from "../components/Dashboard";
import Modals from "../components/Modals";
---

<Layout title="API Keys - Pouch AI">
    <div class="max-w-5xl mx-auto py-16 px-6">
        {/* Header - Static HTML */}
        <div
            class="flex flex-col sm:flex-row sm:justify-between sm:items-end gap-8 mb-12"
        >
            <div class="space-y-1">
                <h1 class="text-4xl font-extrabold text-white tracking-tight">
                    API Keys
                </h1>
                <p class="text-base-content/40 font-medium">
                    Manage your application's access to LLM providers.
                </p>
            </div>
            <label
                for="create-key-modal"
                class="btn btn-primary rounded-2xl px-8 h-14 shadow-2xl shadow-primary/25 gap-3 group border-none transform hover:scale-[1.02] active:scale-95 transition-all"
            >
                <div
                    class="p-1 rounded-lg bg-white/20 group-hover:rotate-90 transition-transform duration-500"
                >
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="h-4 w-4 text-white"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2.5"
                            d="M12 4v16m8-8H4"></path>
                    </svg>
                </div>
                <span class="font-bold uppercase tracking-widest text-xs"
                    >Generate API Key</span
                >
            </label>
        </div>

        {/* Dynamic Dashboard - Client Island */}
        <Dashboard client:load />
    </div>

    {/* Modals - Client Island */}
    <Modals client:load />
</Layout>
